{
  "tasks": {
    "dev": "deno run --allow-net --allow-env --allow-read --watch main.ts",
    "run": "deno run --allow-net --allow-env --allow-read main.ts",
    "format": "deno fmt . && deno lint .",
    "database": "deno run --allow-net --allow-env --allow-read --watch ./database/mongo.ts"
  }
}
